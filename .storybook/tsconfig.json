/* Typechecking context for Storybook config and stories (stories are excluded from the library build). */
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["./*.ts", "../projects/ui/src/**/*.ts"],
  "exclude": ["../projects/ui/src/**/*.spec.ts", "../projects/ui/src/test-setup.ts"]
}
